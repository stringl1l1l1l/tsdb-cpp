hf:
  dataDir: data
  jsonDir: data
  fileNameFormat: "{prefix}-{index}.zst"
  timestampsFileNamePrefix: timestamps
  valuesFileNamePrefix: values
  
  compress:
    outBufferSize: 40960
    compressionLevel: 0
